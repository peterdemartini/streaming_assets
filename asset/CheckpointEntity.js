'use strict';

/**
 * id - This slice_id for the checkpoint
 */
const CheckpointEntity = function CheckpointEntity(id) {
    this.id = id;
};

/* StreamEntity.prototype.x = function x(value) {
}; */

module.exports = CheckpointEntity;
